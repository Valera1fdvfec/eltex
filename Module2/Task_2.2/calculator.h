#ifndef CALCULATOR_H
#define CALCULATOR_H

#include <stdio.h>
#include <stdarg.h>
#include <string.h>

double add(int count, ...);
double subtract(int count, ...);
double multiply(int count, ...);
double divide(int count, ...);

#endif // CALCULATOR_H
